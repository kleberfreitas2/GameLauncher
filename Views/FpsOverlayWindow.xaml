<Window x:Class="GameLauncher.Views.FpsOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FPS Overlay"
        Width="120" Height="50"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual">
    <Border Background="#CC0A0A18" CornerRadius="10" Padding="12,6"
            HorizontalAlignment="Center" VerticalAlignment="Center">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="2" Opacity="0.7"/>
        </Border.Effect>
        <StackPanel Orientation="Horizontal">
            <TextBlock x:Name="FpsText" Text="--" FontSize="22" FontWeight="Bold"
                       Foreground="#00E676" VerticalAlignment="Center"/>
            <TextBlock Text=" FPS" FontSize="11" FontWeight="Bold"
                       Foreground="#8888AA" VerticalAlignment="Center" Margin="3,3,0,0"/>
        </StackPanel>
    </Border>
</Window>
