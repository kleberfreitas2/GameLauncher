<Window x:Class="GameLauncher.Views.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GLauncher"
        Icon="/Assets/icon.ico"
        WindowStyle="None"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Background="#0D0D0D"
        ResizeMode="NoResize">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="40" ResizeBorderThickness="6" GlassFrameThickness="0" CornerRadius="0"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <QuadraticEase x:Key="EaseInOut" EasingMode="EaseInOut"/>
    </Window.Resources>

    <Grid>
        <!-- Background apenas com imagem aleatória (sem gradiente) -->
        <Image x:Name="BackgroundImage" 
               Stretch="UniformToFill" 
               Opacity="1"
               RenderOptions.BitmapScalingMode="HighQuality"/>

        <!-- Overlay escuro (80%) para destacar o logotipo -->
        <Rectangle Fill="#CC000000"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top"
                    Margin="0,0,0,0" WindowChrome.IsHitTestVisibleInChrome="True">
            
            <Button x:Name="BtnMinimize" Click="BtnMinimize_Click" Width="46" Height="36"
                    Background="Transparent" BorderThickness="0" Cursor="Hand" Focusable="False">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}">
                            <TextBlock Text="—" Foreground="#AAAAAA" FontSize="13" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-2,0,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            
            <Button x:Name="BtnMaxRestore" Click="BtnMaxRestore_Click" Width="46" Height="36"
                    Background="Transparent" BorderThickness="0" Cursor="Hand" Focusable="False">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}">
                            
                            <Canvas Width="12" Height="12" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Rectangle Width="8" Height="8" Stroke="#AAAAAA" StrokeThickness="1.2" Canvas.Left="3" Canvas.Top="0" Fill="Transparent"/>
                                <Rectangle Width="8" Height="8" Stroke="#AAAAAA" StrokeThickness="1.2" Canvas.Left="0" Canvas.Top="3" Fill="Transparent"/>
                            </Canvas>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            
            <Button x:Name="BtnClose" Click="BtnClose_Click" Width="46" Height="36"
                    Background="Transparent" BorderThickness="0" Cursor="Hand" Focusable="False">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}">
                            <TextBlock Text="✕" Foreground="#AAAAAA" FontSize="13" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#C42B1C"/>
                                <Setter TargetName="bd" Property="TextElement.Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>

        
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,40">
            
            <Image Source="/Assets/icon.png" Width="80" Height="80" RenderOptions.BitmapScalingMode="HighQuality">
                <Image.RenderTransform>
                    <ScaleTransform x:Name="LogoScale" ScaleX="1" ScaleY="1" CenterX="40" CenterY="40"/>
                </Image.RenderTransform>
                <Image.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                                <DoubleAnimation Storyboard.TargetName="LogoScale"
                                                 Storyboard.TargetProperty="ScaleX"
                                                 From="0.92" To="1.06" Duration="0:0:1.8"
                                                 EasingFunction="{StaticResource EaseInOut}"/>
                                <DoubleAnimation Storyboard.TargetName="LogoScale"
                                                 Storyboard.TargetProperty="ScaleY"
                                                 From="0.92" To="1.06" Duration="0:0:1.8"
                                                 EasingFunction="{StaticResource EaseInOut}"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Image.Triggers>
            </Image>

            
            <TextBlock x:Name="StatusText" Text="Iniciando o GLauncher..."
                       Foreground="#CCCCCC" FontSize="14" FontFamily="Segoe UI"
                       HorizontalAlignment="Center" Margin="0,60,0,0"/>
        </StackPanel>

        
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,80">
            <Ellipse x:Name="Dot1" Width="6" Height="6" Fill="#555555" Margin="4,0">
                <Ellipse.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard RepeatBehavior="Forever">
                                <ColorAnimation Storyboard.TargetName="Dot1" Storyboard.TargetProperty="(Ellipse.Fill).(SolidColorBrush.Color)"
                                                From="#555555" To="#CCCCCC" Duration="0:0:0.4" BeginTime="0:0:0" AutoReverse="True"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Ellipse.Triggers>
            </Ellipse>
            <Ellipse x:Name="Dot2" Width="6" Height="6" Fill="#555555" Margin="4,0">
                <Ellipse.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard RepeatBehavior="Forever">
                                <ColorAnimation Storyboard.TargetName="Dot2" Storyboard.TargetProperty="(Ellipse.Fill).(SolidColorBrush.Color)"
                                                From="#555555" To="#CCCCCC" Duration="0:0:0.4" BeginTime="0:0:0.2" AutoReverse="True"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Ellipse.Triggers>
            </Ellipse>
            <Ellipse x:Name="Dot3" Width="6" Height="6" Fill="#555555" Margin="4,0">
                <Ellipse.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard RepeatBehavior="Forever">
                                <ColorAnimation Storyboard.TargetName="Dot3" Storyboard.TargetProperty="(Ellipse.Fill).(SolidColorBrush.Color)"
                                                From="#555555" To="#CCCCCC" Duration="0:0:0.4" BeginTime="0:0:0.4" AutoReverse="True"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Ellipse.Triggers>
            </Ellipse>
        </StackPanel>
    </Grid>
</Window>
