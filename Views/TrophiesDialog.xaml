<Window x:Class="GameLauncher.Views.TrophiesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Trofeus" WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" ResizeMode="NoResize"
        Width="740" Height="620" WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Window.Resources>
        <Style x:Key="FilterBtnStyle" TargetType="Button">
            <Setter Property="Background" Value="#1A2233"/>
            <Setter Property="Foreground" Value="#8899AA"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="20" Padding="{TemplateBinding Padding}">
                            <TextBlock Text="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}"
                                       FontSize="{TemplateBinding FontSize}" FontFamily="{TemplateBinding FontFamily}"
                                       HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#253045"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border CornerRadius="20" Background="#0D1117" ClipToBounds="True">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="40" ShadowDepth="0" Opacity="0.8"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" CornerRadius="20,20,0,0" Padding="28,20,20,20">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#1A1040" Offset="0"/>
                        <GradientStop Color="#0D1117" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <md:PackIcon Kind="Trophy" Width="28" Height="28" Foreground="#FFD700" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBlock FontSize="24" FontWeight="Black" FontFamily="Segoe UI Black" VerticalAlignment="Center">
                                <Run Text="Trofeus" Foreground="White"/>
                                <Run Text=" GLauncher" Foreground="#FFD700"/>
                            </TextBlock>
                        </StackPanel>
                        <TextBlock x:Name="SummaryText" FontSize="13" Foreground="#8899AA" FontFamily="Segoe UI"/>
                    </StackPanel>
                    <Button Grid.Column="1" Click="CloseBtn_Click" Cursor="Hand" Background="Transparent" BorderThickness="0" VerticalAlignment="Top" Padding="8">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="Bd" Background="Transparent" CornerRadius="8" Padding="6">
                                    <md:PackIcon Kind="Close" Width="18" Height="18" Foreground="#667788"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#1A2233"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>
            <StackPanel Grid.Row="1" Margin="28,0,28,14">
                <Grid Margin="0,0,0,6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="ProgressLabel" Grid.Column="0" FontSize="12" Foreground="#667788" FontFamily="Segoe UI"/>
                    <TextBlock x:Name="PercentLabel" Grid.Column="1" FontSize="12" FontWeight="Bold" Foreground="#FFD700" FontFamily="Segoe UI"/>
                </Grid>
                <Border Height="8" CornerRadius="4" Background="#1A2233">
                    <Border x:Name="ProgressFill" Height="8" CornerRadius="4" HorizontalAlignment="Left" Width="0">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#FFD700" Offset="0"/>
                                <GradientStop Color="#00E676" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                    </Border>
                </Border>
            </StackPanel>
            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="28,0,28,16">
                <Button x:Name="FilterAll"      Content="Todos"     Tag="All"      Click="Filter_Click" Style="{StaticResource FilterBtnStyle}" Margin="0,0,6,0"/>
                <Button x:Name="FilterBronze"   Content="Bronze"    Tag="Bronze"   Click="Filter_Click" Style="{StaticResource FilterBtnStyle}" Margin="0,0,6,0"/>
                <Button x:Name="FilterSilver"   Content="Prata"     Tag="Silver"   Click="Filter_Click" Style="{StaticResource FilterBtnStyle}" Margin="0,0,6,0"/>
                <Button x:Name="FilterGold"     Content="Ouro"      Tag="Gold"     Click="Filter_Click" Style="{StaticResource FilterBtnStyle}" Margin="0,0,6,0"/>
                <Button x:Name="FilterPlatinum" Content="Platina"   Tag="Platinum" Click="Filter_Click" Style="{StaticResource FilterBtnStyle}" Margin="0,0,6,0"/>
                <Button x:Name="FilterUnlocked" Content="Obtidos"   Tag="Unlocked" Click="Filter_Click" Style="{StaticResource FilterBtnStyle}"/>
            </StackPanel>
            <ScrollViewer x:Name="TrophyScroll" Grid.Row="3" VerticalScrollBarVisibility="Auto" Margin="14,0,14,14">
                <ItemsControl x:Name="TrophyList">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
