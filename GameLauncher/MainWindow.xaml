<Window x:Class="GameLauncher.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes" Title="Game Launcher EXP." Icon="Assets/icon.ico" Height="700" Width="1200" MinHeight="500" MinWidth="800" WindowStartupLocation="CenterScreen" TextElement.Foreground="{DynamicResource MaterialDesignBody}" TextElement.FontFamily="{DynamicResource MaterialDesignFont}" Background="{DynamicResource AppBackgroundBrush}">
  <Grid>
    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
    <Border Grid.Row="0" Background="{DynamicResource HeaderBackgroundBrush}" Padding="24,14"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,20,0"><TextBlock Text="GAME LAUNCHER EXP." FontSize="22" FontWeight="Bold" Foreground="White"/><TextBlock Text="Seus jogos favoritos em um so lugar" FontSize="11" Foreground="#888888" Margin="0,3,0,0"/></StackPanel><Border Grid.Column="1" CornerRadius="8" Background="#0D0D1A" BorderBrush="#333355" BorderThickness="1" VerticalAlignment="Center"><Grid Margin="10,0"><md:PackIcon Kind="Magnify" Width="16" Height="16" Foreground="#666688" VerticalAlignment="Center" HorizontalAlignment="Left"/><TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" Foreground="White" CaretBrush="White" BorderThickness="0" FontSize="13" Height="40" Padding="24,0,10,0" VerticalContentAlignment="Center" md:HintAssist.Hint="Buscar jogo..." md:HintAssist.Foreground="#555577" Style="{StaticResource MaterialDesignTextBox}"/></Grid></Border><StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center"><Button Content="Fundo" Command="{Binding ChangeBackgroundCommand}" Style="{StaticResource MaterialDesignFlatButton}" Foreground="{DynamicResource AccentGreenBrush}" FontWeight="Bold" Margin="0,0,10,0"/><Button Content="Tema" Command="{Binding OpenThemeCommand}" Style="{StaticResource MaterialDesignFlatButton}" Foreground="{DynamicResource AccentGreenBrush}" FontWeight="Bold" Margin="0,0,10,0"/><Button Content="+ ADICIONAR JOGO" Command="{Binding AddGameCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Background="{DynamicResource AccentBrush}" BorderBrush="{DynamicResource AccentBrush}" Foreground="White" Padding="20,10" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,0,0"/></StackPanel></Grid></Border>
    <Grid Grid.Row="1">
      <Image Source="{Binding BackgroundImagePath, Converter={StaticResource PathToImageConverter}}" Stretch="UniformToFill" Opacity="0.22" Visibility="{Binding HasBackgroundImage, Converter={StaticResource BoolToVisibility}}" Panel.ZIndex="0"/>
      <Rectangle Fill="#0D0D0D" Opacity="0.45" Visibility="{Binding HasBackgroundImage, Converter={StaticResource BoolToVisibility}}" Panel.ZIndex="1"/>
      <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"><StackPanel.Style><Style TargetType="StackPanel"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding Games.Count}" Value="0"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></StackPanel.Style>
        <md:PackIcon Kind="GamepadVariantOutline" Width="100" Height="100" Foreground="#2A2A2A" HorizontalAlignment="Center"/>
        <TextBlock Text="Nenhum jogo adicionado ainda" FontSize="20" Foreground="#555555" HorizontalAlignment="Center" Margin="0,20,0,0"/>
        <TextBlock Text="Clique em + ADICIONAR JOGO para comecar" FontSize="13" Foreground="#444444" HorizontalAlignment="Center" Margin="0,8,0,0"/>
      </StackPanel>
      <ScrollViewer Padding="30,20" VerticalScrollBarVisibility="Auto"><ScrollViewer.Style><Style TargetType="ScrollViewer"><Setter Property="Visibility" Value="Visible"/><Style.Triggers><DataTrigger Binding="{Binding Games.Count}" Value="0"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger></Style.Triggers></Style></ScrollViewer.Style>
        <ItemsControl ItemsSource="{Binding GamesView}"><ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel Orientation="Horizontal"/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
          <ItemsControl.ItemTemplate><DataTemplate>
            <Border Width="185" Height="265" Margin="10" CornerRadius="10" Cursor="Hand" Background="{DynamicResource CardBackgroundBrush}">
              <Border.Style><Style TargetType="Border"><Style.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter Property="Background"><Setter.Value><SolidColorBrush Color="#2A2A4A"/></Setter.Value></Setter><Setter Property="Effect"><Setter.Value><DropShadowEffect Color="#7C4DFF" BlurRadius="22" Opacity="0.55"/></Setter.Value></Setter></Trigger><DataTrigger Binding="{Binding IsFavorite}" Value="True"><Setter Property="Effect"><Setter.Value><DropShadowEffect Color="#FFD700" BlurRadius="22" ShadowDepth="0" Opacity="0.7"/></Setter.Value></Setter></DataTrigger></Style.Triggers></Style></Border.Style>
              <Grid><Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="95"/></Grid.RowDefinitions>
                <Button Grid.RowSpan="2" Command="{Binding DataContext.LaunchGameCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" Background="Transparent" BorderThickness="0" Padding="0" Width="185" Height="265" Style="{StaticResource MaterialDesignFlatButton}">
                  <Grid Width="185" Height="265"><Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="95"/></Grid.RowDefinitions>
                    <Border Grid.Row="0" CornerRadius="10,10,0,0" ClipToBounds="True" Background="{DynamicResource CardImageBackgroundBrush}">
                      <Grid>
                        <md:PackIcon Kind="GamepadVariant" Width="75" Height="75" Foreground="#7C4DFF" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding HasNoImage, Converter={StaticResource BoolToVisibility}}"/>
                        <Image Visibility="{Binding HasImage, Converter={StaticResource BoolToVisibility}}" Source="{Binding EffectiveImagePath, Converter={StaticResource PathToImageConverter}}" Stretch="UniformToFill"/>
                      </Grid>
                    </Border>
                    <StackPanel Grid.Row="1" Margin="14,10" VerticalAlignment="Top"><TextBlock Text="{Binding DisplayName}" FontSize="13" FontWeight="SemiBold" Foreground="White" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis"/><StackPanel Orientation="Horizontal" Margin="0,5,0,0"><md:PackIcon Kind="ClockOutline" Width="11" Height="11" Foreground="#9999BB" VerticalAlignment="Center" Margin="0,0,3,0"/><TextBlock Text="{Binding LastPlayedText}" FontSize="10" Foreground="#9999BB" VerticalAlignment="Center"/></StackPanel><TextBlock Text="Jogar" FontSize="11" Foreground="#00E676" Margin="0,5,0,0" FontWeight="SemiBold"/></StackPanel>
                  </Grid>
                </Button>
                <StackPanel Grid.Row="0" HorizontalAlignment="Right" VerticalAlignment="Top" Orientation="Horizontal" Margin="0,6,6,0"><Button Width="28" Height="28" Padding="0" Command="{Binding DataContext.ToggleFavoriteCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" Style="{StaticResource MaterialDesignFlatButton}" Background="#22223A" BorderThickness="0" ToolTip="Favorito" Margin="0,0,3,0"><md:PackIcon Kind="{Binding FavoriteIcon}" Width="14" Height="14" Foreground="{Binding FavoriteColor}"/></Button><Button Width="28" Height="28" Padding="0" Command="{Binding DataContext.ChangeImageCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" Style="{StaticResource MaterialDesignFlatButton}" Background="#333355" BorderThickness="0" ToolTip="Alterar imagem" Margin="0,0,3,0"><md:PackIcon Kind="Camera" Width="14" Height="14" Foreground="#AAAAFF"/></Button><Button Width="28" Height="28" Padding="0" Command="{Binding DataContext.SearchCoverCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" Style="{StaticResource MaterialDesignFlatButton}" Background="#223344" BorderThickness="0" ToolTip="Buscar capa online" Margin="0,0,3,0"><md:PackIcon Kind="Magnify" Width="14" Height="14" Foreground="#66AAFF"/></Button><Button Width="28" Height="28" Padding="0" Command="{Binding DataContext.RenameGameCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" Style="{StaticResource MaterialDesignFlatButton}" Background="#223322" BorderThickness="0" ToolTip="Renomear" Margin="0,0,3,0"><md:PackIcon Kind="Pencil" Width="14" Height="14" Foreground="#88FF88"/></Button><Button Width="28" Height="28" Padding="0" Command="{Binding DataContext.RemoveGameCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" Style="{StaticResource MaterialDesignFlatButton}" Background="#AA2222" BorderThickness="0" ToolTip="Remover jogo"><md:PackIcon Kind="Close" Width="14" Height="14" Foreground="White"/></Button></StackPanel>
              </Grid>
            </Border>
          </DataTemplate></ItemsControl.ItemTemplate>
        </ItemsControl></ScrollViewer>
    </Grid>
    <Border Grid.Row="2" Background="#16213E" Padding="30,10"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="{Binding StatusMessage}" Foreground="#888888" FontSize="12" VerticalAlignment="Center"/><TextBlock Grid.Column="1" Text="Desenvolvido por Kleber Freitas" Foreground="#555577" FontSize="11" VerticalAlignment="Center"/></Grid></Border>
  </Grid>
</Window>
